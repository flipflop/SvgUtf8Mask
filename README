This is only a project for testing out a number of options for creating a dynamic image mask.

Includes dynamic Base64 and Utf8 encoding of strings. 

Uses -webkit-mask, so kinda Webkit based right now.



